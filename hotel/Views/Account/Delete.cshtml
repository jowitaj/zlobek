@model zlobek.Entities.Account

<h2>Delete Account</h2>
<form asp-action="/Account/Delete" asp-controller="Account" method="post">
<div class="form-group">
    <label for="AccountId" class="control-label">Account Id:</label>
    <input type="text" name="AccountId" class="form-control" />
    <input type="hidden" name="AccountId" value="@ViewData["AccountId"]" />
</div>
    <div class="form-group">
            <button type="submit" class="btn btn-primary">Submit</button>
        </div>
</form>